#include <bits/stdc++.h>
using namespace std;
void quickSort(int n, int *a){
    if (n <= 1)  return;
    int pivot = 0, too_big_index = 1, too_small_index = n-1;
    while(1){
        while (a[too_big_index] <= a[pivot])    too_big_index++;
        while (a[too_small_index] >)
    }
}
int main(){

    return 0;
}
